# Environment Variables for QNow Backend
# This is a template file. Copy this to .env and fill in your actual values.
# For Render deployment, set these as environment variables in the Render dashboard.

# Server Configuration
PORT=3000
NODE_ENV=development

# Supabase Configuration
# Get these from your Supabase project dashboard > Settings > API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_supabase_anon_public_key_here

# JWT Secret
# Use your Supabase JWT Secret from Settings > API > JWT Secret
# Or generate a random one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here

# Email Configuration (Required for password reset)
# Option 1: SendGrid (recommended for production like Render)
# Sign up at https://sendgrid.com (free tier: 100 emails/day)
# Get API key from Settings > API Keys
SENDGRID_API_KEY=your_sendgrid_api_key_here
EMAIL_USER=your_email@gmail.com

# Option 2: Gmail (for local development only, won't work on Render)
# Use Gmail App Password (NOT your regular Gmail password)
# Setup: https://myaccount.google.com/apppasswords
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASS=your_16_char_app_password_here

# Application URL (for password reset links)
# Local development: http://localhost:3000
# Render production: https://your-service-name.onrender.com
APP_URL=http://localhost:3000

# Firebase Configuration (for push notifications)
# Path to your Firebase service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./path/to/firebase-service-account.json
